export default function HeroSection({ kirim, university }) {
  return (
    <div>
      HeroSection
      <div>kelas : {kirim}</div>
      <div>university Ajeng : {university}</div>
    </div>
  );
}
